# Copiez ce fichier en ".env" pour un test EN LOCAL uniquement (jamais commité sur GitHub).
# Sur Render, ces valeurs se configurent dans l'onglet "Environment" du service, pas dans un fichier.

GEMINI_API_KEY=votre_cle_api_google_ai_studio
GEMINI_MODEL=gemini-3.5-flash
PORT=3000

